import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  gap: 10px;
  position: absolute;
  left: 0;
  margin-top: -40px;
  & > svg {
    cursor: pointer;
  }
`;
